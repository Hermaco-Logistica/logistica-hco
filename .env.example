# Frontend (opcional)
# Si no se define, el frontend usa /api/tracking-status por defecto.
VITE_TRACKING_STATUS_URL=/api/tracking-status

# Serverless (Netlify/Vercel): NO usar prefijo VITE_
# Estas variables viven solo en el runtime del servidor.
DHL_TRACKING_API_URL=
DHL_API_KEY=
DHL_API_SECRET=
